<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reja</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
      integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T"
      crossorigin="anonymous"
    />

    <style>
      /*h1*/
      .display-4 {
        width: 500px;
        margin-left: 329px;
        margin-top: 45px;
        border-radius: 50px;
        font-family: "Poppins", sans-serif;
        font-size: 1.5rem;

        background: linear-gradient(to right, #75a2f4, #5e5e5e);
        text-transform: uppercase;
        font-weight: bold;
        text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
        margin-bottom: 20px;
        display: inline-block;
        opacity: 0;
        animation: fadeInText 2s forwards;
        animation-delay: 0.5s;
      }

      .letter {
        margin-left: 12px;
        display: inline-block;
        opacity: 0;
        animation: fadeInLetter 0.1s forwards;
      }

      @keyframes fadeInLetter {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes fadeInText {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      } /* Input Field Styling */
      #create-field {
        padding: 10px 15px;
        border: 2px solid #87ceeb;
        border-radius: 30px;
        outline: none;
        transition: all 0.3s ease-in-out;
        background-color: #f8f8ff;
        font-size: 16px;
        color: #333;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }

      #create-field:focus {
        border-color: #6495ed;
        box-shadow: 0 0 10px #6495ed;
      }

      .btn {
        padding: 10px 20px;
        font-size: 16px;
        border: none;
        border-radius: 25px;
        cursor: pointer;
        transition: all 0.3s ease-in-out;
        color: #fff;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      }

      .btn-primary {
        background-color: #32cd32; /* Lime Green */
      }

      .btn-primary:hover {
        background-color: #228b22; /* Forest Green */
        transform: scale(1.1);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
      }

      .btn-secondary {
        background-color: #ffa07a; /* Light Salmon */
      }

      .btn-secondary:hover {
        background-color: #ff6347; /* Tomato */
        transform: scale(1.1);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
      }

      .btn-danger {
        background-color: #ff4500; /* Orange Red */
      }

      .btn-danger:hover {
        background-color: #b22222; /* Firebrick */
        transform: scale(1.1);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
      }

      #clean-all {
        background-color: #4682b4; /* Steel Blue */
      }

      #clean-all:hover {
        background-color: #4169e1; /* Royal Blue */
        transform: scale(1.1);
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
      }
    </style>
  </head>
  <body style="background-color: #b8bbd1">
    <div class="container">
      <h1
        style="display: flex; flex-direction: row; justify-content: center"
        class="display-4 text-center py-1"
      >
        <span class="letter" style="animation-delay: 0.1s">R</span>
        <span class="letter" style="animation-delay: 0.2s">E</span>
        <span class="letter" style="animation-delay: 0.3s">J</span>
        <span class="letter" style="animation-delay: 0.4s">A</span>
        <span class="letter" style="animation-delay: 0.6s"> </span>
        <span class="letter" style="animation-delay: 0.7s">V</span>
        <span class="letter" style="animation-delay: 0.8s">A</span>
        <span class="letter" style="animation-delay: 1s"> </span>
        <span class="letter" style="animation-delay: 1.1s">H</span>
        <span class="letter" style="animation-delay: 1.2s">A</span>
        <span class="letter" style="animation-delay: 1.3s">B</span>
        <span class="letter" style="animation-delay: 1.4s">A</span>
        <span class="letter" style="animation-delay: 1.5s">R</span>
        <span class="letter" style="animation-delay: 1.6s">L</span>
        <span class="letter" style="animation-delay: 1.7s">A</span>
        <span class="letter" style="animation-delay: 1.8s">R</span>
        <span class="letter" style="animation-delay: 1.9s">!</span>
      </h1>
      <form id="create-form" action="/create-item" method="POST">
        <div
          class="d-flex align-items-center"
          style="background-color: #edeff1; height: 70px; border-radius: 25px"
        >
          <input
            id="create-field"
            name="reja"
            autofocus
            autocomplete="off"
            class="form-control mr-3"
            type="text"
            style="flex: 1; margin-left: 15px"
          />
          <button
            class="btn btn-primary"
            style="border-radius: 20px; margin-right: 15px"
          >
            Habar qoldirish!
            <!-- Yangi Reja Qo`shish -->
          </button>
        </div>
      </form>
      <div class="btn-for-delete">
        <!-- <% if (items && items.length > 0) { %> <%= items[0].reja %> <% } else {
        %><% } %> -->

        <ul id="item-list" class="list-group pb-5">
          <% items.map(function(item) { %>
          <li
            style="margin-top: 20px; border-radius: 40px"
            class="list-group-item list-group-item-info d-flex align-items-center justify-content-between"
          >
            <span class="item-text"><%= item.reja %></span>
            <div>
              <button
                data-id="<%= item._id %>"
                class="edit-me btn btn-secondary btn-sm mr-1"
              >
                O`zgartirish
              </button>
              <button
                data-id="<%= item._id %>"
                class="delete-me btn btn-danger btn-sm"
              >
                O`chirish
              </button>
            </div>
          </li>
          <% }) %>
        </ul>
        <button
          id="clean-all"
          class="btn btn-danger"
          style="border-radius: 20px"
        >
          Hamma Habarlarni O`chirish
        </button>
      </div>
    </div>
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <script src="/browser.js"></script>
  </body>
</html>
